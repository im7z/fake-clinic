<nav class="navbar navbar-expand-lg navbar-dark bg-primary shadow-sm">
  <div class="container-fluid">

    <!-- Brand -->
    <a class="navbar-brand fw-semibold" href="/user/available?userName=<%= userName %>&phone=<%= phone %>">
       QU Clinic
    </a>

    <!-- Toggler (THIS IS WHAT WAS MISSING) -->
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#userNavbar"
      aria-controls="userNavbar" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>

    <!-- Collapsible Links -->
    <div class="collapse navbar-collapse" id="userNavbar">
      <div class="navbar-nav ms-auto">

        <a class="nav-link <%= page === 'available' ? 'active' : '' %>"
          href="/user/available?userName=<%= userName %>&phone=<%= phone %>">
          Appointments
        </a>

        <a class="nav-link <%= page === 'doctors' ? 'active' : '' %>"
          href="/user/doctors?userName=<%= userName %>&phone=<%= phone %>">
          Doctors
        </a>

        <a class="nav-link <%= page === 'past' ? 'active' : '' %>"
          href="/user/past?userName=<%= userName %>&phone=<%= phone %>">
          History
        </a>

        <a class="nav-link <%= page === 'loyalty' ? 'active' : '' %>"
          href="/user/loyalty?userName=<%= userName %>&phone=<%= phone %>">
          Loyalty Point
        </a>

        <a class="nav-link text-danger fw-semibold" href="/login">Logout</a>

      </div>
    </div>

  </div>
</nav>





<!-- User Popup Modal -->
<div class="modal fade" id="userPopup" tabindex="-1" aria-hidden="true">
  <div class="modal-dialog">
    <div class="modal-content">

      <div class="modal-header">
        <h5 class="modal-title" id="userPopupTitle">تنبيه</h5>
        <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
      </div>

      <!-- FIXED DIRECTION HERE -->
      <div class="modal-body" style="direction: rtl; text-align: right;">
        <p id="userPopupText"></p>
      </div>

      <div class="modal-footer">
        <button type="button" class="btn btn-primary" data-bs-dismiss="modal">حسناً</button>
      </div>

    </div>
  </div>
</div>
