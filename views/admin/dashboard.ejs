<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <title>Admin Dashboard</title>

  <!-- Bootstrap -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">

  <!-- Reuse main user/admin theme -->
  <link rel="stylesheet" href="/css/user.css">
</head>

<body class="bg-light">

  <!-- 🔹 Modern Admin Navbar -->
  <%- include('../partials/_navbarr', { page: 'dashboard' }) %>

    <div class="container py-4">

      <!-- Page Title -->
      <h2 class="text-center mb-4 fw-bold"> Admin Dashboard</h2>

      <!-- MARK ATTENDANCE CARD -->
      <div class="card p-4 shadow-sm mb-4">
        <h5 class="mb-3  fw-bold"> Mark Attendance / Missed</h5>

        <form id="attendanceForm" class="row g-3 mb-3">
          <div class="col-md-6">
            <input type="text" id="attendanceUser" class="form-control" placeholder="Enter username…" required>
          </div>

          <div class="col-md-6 d-grid">
            <button class="btn btn-primary" type="submit">
              Load User Appointments
            </button>
          </div>
        </form>

        <!-- User Appointments Will Show Here -->
        <div id="attendanceList" class="mt-3 attendance-grid"></div>
        

      </div>
        <!-- MANAGE AVAILABLE BUTTON -->
        <div class="text-center mt-4">
          <a href="/admin/add-block" class="btn btn-lg btn-success shadow-sm">
            Manage Available Appointments
          </a>
        </div>

      <!-- JS -->
      <script>const API = "<%= API %>";</script>
      <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
      <script src="/js/admin.js"></script>
</body>

</html>